# AGENTS.md

## 项目概述

这是一个新建的 Go 语言项目目录，名称为 `game_control`，预计用于开发游戏控制相关的应用程序。

**当前状态：** 项目尚未初始化，这是一个全新的空目录。

## 项目类型

- **编程语言：** Go (Golang)
- **项目类型：** 代码项目（待初始化）

## 目录结构

```
game_control/
└── AGENTS.md
```

## 开发环境

- **操作系统：** Linux (WSL2)
- **Go 版本：** 需要确认
- **当前状态：** 项目未初始化

## 项目初始化（TODO）

此项目需要初始化。建议的初始化步骤：

1. **初始化 Go 模块：**
   ```bash
   go mod init github.com/yourusername/game_control
   ```

2. **创建基础项目结构：**
   - `main.go` - 主程序入口
   - `cmd/` - 命令行工具
   - `pkg/` - 可复用的包
   - `internal/` - 内部包
   - `config/` - 配置文件

3. **添加必要的依赖：**
   - 根据项目需求添加相关包

## 构建和运行（待定）

由于项目尚未初始化，具体的构建和运行命令待项目创建后确定。

## 开发约定（待定）

项目的编码风格、测试实践和贡献指南将在项目开发过程中建立。